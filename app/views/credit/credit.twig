<!DOCTYPE HTML>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="pl" lang="pl">
<head>
<meta charset="utf-8" />
<link rel="stylesheet" href="http://yui.yahooapis.com/pure/0.6.0/pure-min.css">
<title>Kalkulator</title>
</head>
<body>
    {{ menu|raw }}
    <form action="/app/views/credit/calc.php" method="post" class="pure-form pure-form-stacked">
        <label for="id_x">Kwota: </label>
        <input id="id_x" type="number" name="amount" value="{{ amount }}" /><br />
        <label for="id_x">Lata: </label>
        <input id="id_x" type="number" name="years" value="{{ years }}" /><br />

        <label for="id_op">Oprocentowanie: </label>
        <input id="id_op" type="text" name="interest"
            inputmode="decimal"
            pattern="[0-9]+([.,][0-9]+)?"
            title="Użyj przecinka lub kropki jako separatora dziesiętnego"
            value="{{ interest }}" /><br />
        <input type="submit" value="Oblicz" />
    </form>

    {% if monthlyPayment is not null %}
        <h4>Odsetki: {{ monthlyPayment }}</h4>
    {% endif %}

    {% if errors is not empty %}
        <h4>Wystąpiły błędy:</h4>
        <ol>
        {% for e in errors %}
            <li>{{ e }}</li>
        {% endfor %}
        </ol>
    {% endif %}

</body>
</html>
